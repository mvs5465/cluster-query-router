apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-query-router.fullname" . }}
  labels:
    {{- include "cluster-query-router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-query-router.selectorLabels" . | nindent 4 }}
